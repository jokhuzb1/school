# Project Context

## Stack
- Backend: Fastify (Node.js)
- Frontend: Vite + React (student-register web)
- ORM/DB: Prisma (root project)

## Language
- TypeScript (primary)

## Priority
- Stability & error handling (inferred default)

## Skills to Use
- global-config (always)
- react-vite (Vite frontend)
- errors (error handling patterns)
- security (credentials, uploads, API safety)
- typescript (type safety)
- api-patterns (Fastify endpoints, REST)
- testing (when adding/modifying logic)

## Notes
- Repo contains a separate `student-register` module (Fastify server + Vite web) for Face ID registration.

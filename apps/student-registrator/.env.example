# Student Registrator Environment (development)

# Main backend URL (login + API)
# VITE_BACKEND_URL=https://school-crm-api.duckdns.org

# Optional: keep verbose fetch diagnostics in browser storage
VITE_ENABLE_NETWORK_DEBUG=false

# Optional: fetch timeout in milliseconds
VITE_FETCH_TIMEOUT_MS=20000

# Optional: provisioning auth token (matches backend PROVISIONING_TOKEN)
VITE_BACKEND_TOKEN=

# Optional: school scope for provisioning
VITE_SCHOOL_ID=

# Local credentials/device limit (default: 10)
# Frontend (Vite) and Tauri backend both read this value.
VITE_DEVICE_CREDENTIALS_LIMIT=10
DEVICE_CREDENTIALS_LIMIT=10

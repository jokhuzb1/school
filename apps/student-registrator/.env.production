# Student Registrator Environment (production build)

# Main backend URL baked into EXE on build
VITE_BACKEND_URL=https://school-crm-api.duckdns.org:6969

# Optional: provisioning auth token (matches backend PROVISIONING_TOKEN)
# VITE_BACKEND_TOKEN=

# Optional: school scope for provisioning
# VITE_SCHOOL_ID=
